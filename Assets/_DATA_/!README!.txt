Nothing in this folder should be referenced outside of editor scripts!

The _DATA_ folder is an organizational convention that exists to store the verbose definitions that we bake into hardcoded declarations of types like BattleAction at runtime. Nothing in this folder should be referenced in any way in a built game.