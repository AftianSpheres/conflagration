namespace CnfBattleSys
{
    /// <summary>
    /// Defines venues (background models, lighting, environmental effects, etc.)
    /// that can be loaded into the battle scene.
    /// </summary>
    public enum VenueType : short
    {
        InvalidVenue = -1,
        None,
        TestVenue
    }
}