/// <summary>
/// Defines the various BGM tracks that we can
/// get programatically. Doesn't necessarily need to be
/// "every BGM," just (for example) every BGM
/// we might want to pass into the battle scene.
/// </summary>
public enum BGMTrackType : short
{
    InvalidBGMTrack,
    None,
    TestBGM
}